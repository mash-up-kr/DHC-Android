name: Create PR from design-token

on:
  push:
    branches:
      - design
    path:
      - "designTokens.json"

jobs:
  createPullRequest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
